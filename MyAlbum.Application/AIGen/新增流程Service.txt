0. 資料庫的表格名稱：AlbumComment
1. 類別名稱：AlbumComment
2. 類別屬性：
    public Guid AlbumCommentId { get; set; }

    public Guid AlbumPhotoId { get; set; }

    public Guid MemberId { get; set; }

    public string Comment { get; set; } = null!;

    public DateTime ReleaseTimeUtc { get; set; }

    public bool IsChanged { get; set; }

    public byte Status { get; set; }

    public DateTime CreatedAtUtc { get; set; }

    public DateTime UpdatedAtUtc { get; set; }

    public Guid CreatedBy { get; set; }

    public Guid UpdatedBy { get; set; }
	
3. 請設計一個跟新增相關的介面的Service，名稱是：I類別名稱CreateService
4. 介面的Repository一定有1個方法，設定為：
4-1. 方法一定會帶：
4-1-1. Req類別：
4-1-1-1. 名稱：Create<<類別名稱>>Req
4-1-1-2. 指定欄位：
AlbumPhotoId
MemberId
Comment
4-1-1-3. 指定必填欄位：
AlbumPhotoId
MemberId
Comment
4-1-1-4. 如果遇到指定必填欄位有設定，屬性全部都要加[Required]

4-1-2. CancellationToken ct = default

4-2. 方法名稱：Create<<類別名稱>>Async

4-3. 資料庫的表格的PK：AlbumCommentId

4-4. 方法預設會回傳新增資料的PK

5. 請設計一個類別實作介面的Service，名稱是：<<類別名稱>>CreateService
5-1. using、初始實作、建構子、引用Repo一律照以下模式做設定：
using MyAlbum.Shared.Extensions;

/*命名空間略*/
    public class 類別名稱CreateService : BaseService, I類別名稱CreateService
    {
        private readonly ICurrentUserAccessor _currentUser;
        private readonly I類別名稱CreateRepository _mainRepo;
        public 類別名稱CreateService(
            IAlbumDbContextFactory factory,
            ICurrentUserAccessor currentUser,
            I類別名稱CreateRepository mainRepo) : base(factory)
        {
            _mainRepo = mainRepo;
            _currentUser = currentUser;
        }
		
		/*方法*/
	}

5-2. 方法一律public async

5-3. 方法初始一律照以下模式設定：
5-3-1. 取得operatorId：var operatorId = _currentUser.GetRequiredAccountId();
5-3-2. 設定傳入Repo的Dto：
類別名稱CreateDto dto = new 類別名稱CreateDto
{
    欄位 = req.指定欄位
    CreatedBy = operatorId,
    CreatedAtUtc = DateTime.UtcNow,
};
5-4. 設定回傳：
return await _mainRepo.Create類別名稱Async(dto, ct);
