0. 資料庫的表格名稱：AlbumComment
1. 類別名稱：AlbumComment
2. 類別屬性：
    public Guid AlbumCommentId { get; set; }

    public Guid AlbumPhotoId { get; set; }

    public Guid MemberId { get; set; }

    public string Comment { get; set; } = null!;

    public DateTime ReleaseTimeUtc { get; set; }

    public bool IsChanged { get; set; }

    public byte Status { get; set; }

    public DateTime CreatedAtUtc { get; set; }

    public DateTime UpdatedAtUtc { get; set; }

    public Guid CreatedBy { get; set; }

    public Guid UpdatedBy { get; set; }
	
3. 資料庫的表格的PK：AlbumCommentId

4. 請設計一個跟更新相關的介面的Service，名稱是：I類別名稱UpdateService

5. 介面的Service一定有2個方法：
方法1名稱：Update<<類別名稱>>Async
方法2名稱：Update<<類別名稱>>ActiveAsync

6. 方法1設定為：
6-1. 方法一定會帶：
6-1-1. Req類別：
6-1-1-1. 名稱：Update<<類別名稱>>Req
6-1-1-2. 指定欄位：
AlbumCommentId
AlbumPhotoId
MemberId
Comment
Status
6-1-1-3. 指定必填欄位：
AlbumCommentId
AlbumPhotoId
MemberId
Comment
Status
6-1-1-4. 如果遇到指定必填欄位有設定，屬性全部都要加[Required]
6-1-1-5. 遇到 Status關鍵字 請設定為：public Status Status { get; set; }，它在系統裡是Enum

6-1-2. CancellationToken ct = default

7. 方法2設定為：
7-1. 方法一定會帶：
7-1-1. Req類別：
7-1-1-1. 名稱：Update類別名稱ActiveReq
7-1-1-2. 指定欄位：
AlbumCommentId
Status
7-1-1-3. 指定必填欄位：
AlbumCommentId
Status
7-1-1-4. 如果遇到指定必填欄位有設定，屬性全部都要加[Required]
7-1-1-5. 遇到 Status關鍵字 請設定為：public Status Status { get; set; }，它在系統裡是Enum

7-1-2. CancellationToken ct = default

8. 請設計一個類別實作介面的Service，名稱是：<<類別名稱>>UpdateService
8-1. using、初始實作、建構子、引用Repo一律照以下模式做設定：
using MyAlbum.Shared.Extensions;

/*命名空間略*/
    public class 類別名稱UpdateService : BaseService, I類別名稱UpdateService
    {
        private readonly ICurrentUserAccessor _currentUser;
        private readonly I類別名稱UpdateRepository _mainRepo;
        public 類別名稱UpdateService(
            IAlbumDbContextFactory factory,
            ICurrentUserAccessor currentUser,
            I類別名稱UpdateRepository mainRepo) : base(factory)
        {
            _mainRepo = mainRepo;
            _currentUser = currentUser;
        }
		
		/*方法*/
	}
	
8-2. 方法一律public async
8-3. 方法1設定： 
	public async Task<bool> Update類別名稱Async(Update類別名稱Req req, CancellationToken ct = default)
	{
		var operatorId = _currentUser.GetRequiredAccountId();
		類別名稱UpdateDto dto = new 類別名稱UpdateDto();
		dto.屬性 = req.指定屬性;
		dto.UpdatedBy = operatorId;
		return await _mainRepo.Update類別名稱Async(dto, ct);
	}
8-4. 方法2設定： 
	public async Task<bool> Update類別名稱ActiveAsync(Update類別名稱ActiveReq req, CancellationToken ct = default)
	{
		var operatorId = _currentUser.GetRequiredAccountId();
		類別名稱UpdateDto dto = new 類別名稱UpdateDto();
		dto.資料庫的表格的PK = req.指定欄位;
		dto.Status = req.Status;
		dto.UpdatedBy = operatorId;
		return await _mainRepo.Update類別名稱Async(dto, ct);
	}